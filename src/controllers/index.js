const loginCotroller = require('./login.controller');
const userController = require('./user.controller');
const categoryController = require('./category.controller');
const postCotroller = require('./post.controller');

module.exports = {
    postCotroller,
    categoryController,
    userController,
    loginCotroller,
};